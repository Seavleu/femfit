# Build artifacts
node_modules/
.next/
.vercel/
dist/
build/
out/
.turbo/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Environment files (keep .env.example)
.env
.env.local
.env.*.local
!.env.example

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/

# Cursor MCP config (contains tokens — example version is committed)
.cursor/mcp.json
!.cursor/mcp.json.example

# Test artifacts
coverage/
playwright-report/
playwright/.cache/
test-results/

# The polished docx versions — Markdown copies in /docs are the source for AI
*.docx

# Large generated files
*.lock
package-lock.json
yarn.lock
# Keep pnpm-lock.yaml — it's the source of truth for pnpm

# Database dumps
*.sql.gz
*.dump

# Skill bundles, supabase generated types
supabase/.temp/
